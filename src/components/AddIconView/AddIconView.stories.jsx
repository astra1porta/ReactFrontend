import React from "react";

import { AddIconView } from "./AddIconView";

export default {
  title: "Components/Icons/AddIconView",
  component: AddIconView,
};

const Template = () => <AddIconView />;

export const Default = Template.bind({});
